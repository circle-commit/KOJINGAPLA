def extract_text(image_bytes: bytes) -> str:
    # Replace this placeholder with a real OCR model call.
    if not image_bytes:
        return ""

    return "Emergency exit on the left. Keep door closed."
